import Typography from '@mui/material/Typography';

// Full implementation in Step 13
export const AdminUserDetailPage = () => {
  return <Typography variant="title_medium">Admin — User Detail</Typography>;
};
